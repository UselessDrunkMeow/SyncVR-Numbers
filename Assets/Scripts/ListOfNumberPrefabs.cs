using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ListOfNumberPrefabs : MonoBehaviour
{
    //all this script does, is hold a list with all of the connectors in the scene, this lists gets update when another connector is added via the editor script
    public List<NumberConector> _NumberConectors;   
}
